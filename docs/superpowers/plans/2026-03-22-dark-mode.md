# Dark Mode / Light Mode 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Slate(Blue-Gray) 톤 기반 다크/라이트 테마 전환 기능 구현

**Architecture:** `next-themes`의 `ThemeProvider`로 `<html>`에 `.dark` 클래스를 토글하고, globals.css의 CSS 변수를 Slate 팔레트로 교체하여 shadcn/ui 컴포넌트 전체에 자동 적용. `ThemeToggle` 버튼으로 사용자 수동 전환, localStorage에 테마 저장.

**Tech Stack:** Next.js 16, React 19, next-themes 0.4.6, Tailwind CSS v4, shadcn/ui (base-nova), Lucide React, Vitest, Playwright

**Spec:** `docs/superpowers/specs/2026-03-22-dark-mode-design.md`

---

## File Structure

| 파일 | 역할 | 작업 |
|------|------|------|
| `app/globals.css` | CSS 변수 팔레트 (라이트/다크) + 전환 애니메이션 | Modify |
| `app/layout.tsx` | 루트 `<html>` 태그 설정 | Modify |
| `components/providers.tsx` | ThemeProvider 래핑 | Modify |
| `components/layout/theme-toggle.tsx` | 테마 토글 버튼 컴포넌트 | Create |
| `components/layout/header.tsx` | 헤더에 ThemeToggle 삽입 | Modify |
| `__tests__/components/theme-toggle.test.tsx` | ThemeToggle 단위 테스트 | Create |
| `e2e/dark-mode.spec.ts` | E2E 테마 전환 테스트 | Create |

---

### Task 1: Slate 팔레트 CSS 변수 적용

**Files:**
- Modify: `app/globals.css:51-118` (`:root` + `.dark` 블록)

- [ ] **Step 1: `:root` 블록을 Slate Light 팔레트로 교체**

`app/globals.css`의 `:root` 블록(51~84행)을 아래로 교체한다. `--chart-*`와 `--destructive`는 변경하지 않는다.

```css
:root {
  --background: oklch(0.984 0.003 247);
  --foreground: oklch(0.129 0.042 264);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.129 0.042 264);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.129 0.042 264);
  --primary: oklch(0.208 0.042 265);
  --primary-foreground: oklch(0.984 0.003 247);
  --secondary: oklch(0.968 0.007 247);
  --secondary-foreground: oklch(0.208 0.042 265);
  --muted: oklch(0.968 0.007 247);
  --muted-foreground: oklch(0.554 0.022 257);
  --accent: oklch(0.968 0.007 247);
  --accent-foreground: oklch(0.208 0.042 265);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.918 0.011 249);
  --input: oklch(0.918 0.011 249);
  --ring: oklch(0.704 0.022 261);
  --chart-1: oklch(0.87 0 0);
  --chart-2: oklch(0.556 0 0);
  --chart-3: oklch(0.439 0 0);
  --chart-4: oklch(0.371 0 0);
  --chart-5: oklch(0.269 0 0);
  --radius: 0.625rem;
  --sidebar: oklch(0.968 0.007 247);
  --sidebar-foreground: oklch(0.129 0.042 264);
  --sidebar-primary: oklch(0.208 0.042 265);
  --sidebar-primary-foreground: oklch(0.984 0.003 247);
  --sidebar-accent: oklch(0.918 0.011 249);
  --sidebar-accent-foreground: oklch(0.208 0.042 265);
  --sidebar-border: oklch(0.918 0.011 249);
  --sidebar-ring: oklch(0.704 0.022 261);
}
```

- [ ] **Step 2: `.dark` 블록을 Slate Dark 팔레트로 교체**

`app/globals.css`의 `.dark` 블록(86~118행)을 아래로 교체한다.

```css
.dark {
  --background: oklch(0.129 0.042 264);
  --foreground: oklch(0.966 0.007 264);
  --card: oklch(0.208 0.042 265);
  --card-foreground: oklch(0.966 0.007 264);
  --popover: oklch(0.208 0.042 265);
  --popover-foreground: oklch(0.966 0.007 264);
  --primary: oklch(0.918 0.011 249);
  --primary-foreground: oklch(0.208 0.042 265);
  --secondary: oklch(0.279 0.041 260);
  --secondary-foreground: oklch(0.966 0.007 264);
  --muted: oklch(0.279 0.041 260);
  --muted-foreground: oklch(0.704 0.022 261);
  --accent: oklch(0.279 0.041 260);
  --accent-foreground: oklch(0.966 0.007 264);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(0.918 0.011 249 / 12%);
  --input: oklch(0.918 0.011 249 / 18%);
  --ring: oklch(0.554 0.022 257);
  --chart-1: oklch(0.87 0 0);
  --chart-2: oklch(0.556 0 0);
  --chart-3: oklch(0.439 0 0);
  --chart-4: oklch(0.371 0 0);
  --chart-5: oklch(0.269 0 0);
  --sidebar: oklch(0.160 0.042 264);
  --sidebar-foreground: oklch(0.966 0.007 264);
  --sidebar-primary: oklch(0.488 0.243 264.376);
  --sidebar-primary-foreground: oklch(0.966 0.007 264);
  --sidebar-accent: oklch(0.279 0.041 260);
  --sidebar-accent-foreground: oklch(0.966 0.007 264);
  --sidebar-border: oklch(0.918 0.011 249 / 12%);
  --sidebar-ring: oklch(0.554 0.022 257);
}
```

- [ ] **Step 3: 전환 애니메이션 추가**

`@layer base` 블록의 `*` 셀렉터에 transition 추가:

```css
@layer base {
  * {
    @apply border-border outline-ring/50;
    transition: color 0.2s ease, background-color 0.2s ease, border-color 0.2s ease;
  }
  body {
    @apply bg-background text-foreground;
  }
  html {
    @apply font-sans;
  }
}
```

- [ ] **Step 4: 빌드 확인**

```bash
npm run build
```

Expected: 빌드 성공. CSS 변수 파싱 오류 없음.

- [ ] **Step 5: Commit**

```bash
git add app/globals.css
git commit -m "feat: apply Slate (Blue-Gray) color palette for light/dark themes"
```

---

### Task 2: ThemeProvider 연결

**Files:**
- Modify: `app/layout.tsx:23`
- Modify: `components/providers.tsx`

- [ ] **Step 1: `app/layout.tsx`에 `suppressHydrationWarning` 추가**

`app/layout.tsx`의 `<html>` 태그를 수정한다:

```tsx
// Before (line 23)
<html lang="ko" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>

// After
<html lang="ko" suppressHydrationWarning className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
```

`suppressHydrationWarning`은 next-themes가 삽입하는 인라인 스크립트로 인한 React hydration mismatch 경고를 억제한다. `<html>` 태그에만 적용되며, 자식 요소에는 전파되지 않는다.

- [ ] **Step 2: `components/providers.tsx`에 `ThemeProvider` 래핑**

```tsx
"use client";

import { ThemeProvider } from "next-themes";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useState, type ReactNode } from "react";
import { Toaster, toast } from "sonner";

export function Providers({ children }: { readonly children: ReactNode }) {
  const [queryClient] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            retry: 3,
            staleTime: 10_000,
          },
          mutations: {
            onError: (error) => {
              toast.error(error.message ?? "오류가 발생했습니다");
            },
          },
        },
      }),
  );

  return (
    <ThemeProvider
      attribute="class"
      defaultTheme="light"
      enableSystem={false}
      disableTransitionOnChange={false}
    >
      <QueryClientProvider client={queryClient}>
        {children}
        <Toaster position="top-right" />
      </QueryClientProvider>
    </ThemeProvider>
  );
}
```

핵심 props:
- `attribute="class"`: `.dark` CSS 클래스 기반 (globals.css와 일치)
- `defaultTheme="light"`: 첫 방문 시 라이트 모드
- `enableSystem={false}`: 시스템 다크모드 자동 감지 비활성화
- `disableTransitionOnChange={false}`: 전환 시 CSS transition 유지

- [ ] **Step 3: 기존 테스트 통과 확인**

```bash
npm test -- --run
```

Expected: 기존 테스트 전체 통과. `sonner.tsx`의 `useTheme()` 관련 에러가 없어야 한다.

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx components/providers.tsx
git commit -m "feat: wire up ThemeProvider with next-themes"
```

---

### Task 3: ThemeToggle 컴포넌트 (TDD)

**Files:**
- Create: `__tests__/components/theme-toggle.test.tsx`
- Create: `components/layout/theme-toggle.tsx`

- [ ] **Step 1: 테스트 파일 작성**

`__tests__/components/theme-toggle.test.tsx`:

```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { ThemeToggle } from "@/components/layout/theme-toggle";

// next-themes 모킹
const mockSetTheme = vi.fn();
vi.mock("next-themes", () => ({
  useTheme: () => ({
    theme: "light",
    setTheme: mockSetTheme,
  }),
}));

describe("ThemeToggle", () => {
  it("테마_전환_버튼을_렌더링한다", () => {
    render(<ThemeToggle />);
    expect(screen.getByRole("button", { name: "테마 전환" })).toBeDefined();
  });

  it("클릭_시_setTheme을_호출한다", async () => {
    const user = userEvent.setup();
    render(<ThemeToggle />);

    await user.click(screen.getByRole("button", { name: "테마 전환" }));

    expect(mockSetTheme).toHaveBeenCalledWith("dark");
  });
});
```

- [ ] **Step 2: 테스트 실행 — RED 확인**

```bash
npx vitest run __tests__/components/theme-toggle.test.tsx
```

Expected: FAIL — `ThemeToggle` 모듈을 찾을 수 없음.

- [ ] **Step 3: ThemeToggle 컴포넌트 구현**

`components/layout/theme-toggle.tsx`:

```tsx
"use client";

import { useTheme } from "next-themes";
import { Sun, Moon } from "lucide-react";
import { Button } from "@/components/ui/button";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <Button
      variant="ghost"
      size="icon"
      className="relative"
      onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
    >
      <Sun className="h-5 w-5 rotate-0 scale-100 transition-transform dark:rotate-90 dark:scale-0" />
      <Moon className="absolute h-5 w-5 rotate-90 scale-0 transition-transform dark:rotate-0 dark:scale-100" />
      <span className="sr-only">테마 전환</span>
    </Button>
  );
}
```

주의사항:
- `Button`에 `className="relative"` 필수 — Moon의 `absolute` 포지셔닝 기준점
- `dark:` prefix는 Tailwind v4의 `@custom-variant dark` 설정에 의해 `.dark` 클래스 하위에서 활성화

- [ ] **Step 4: 테스트 실행 — GREEN 확인**

```bash
npx vitest run __tests__/components/theme-toggle.test.tsx
```

Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add __tests__/components/theme-toggle.test.tsx components/layout/theme-toggle.tsx
git commit -m "feat: add ThemeToggle component with tests"
```

---

### Task 4: Header에 ThemeToggle 삽입

**Files:**
- Modify: `components/layout/header.tsx:56-63`

- [ ] **Step 1: header.tsx 수정**

`components/layout/header.tsx`에서 import 추가 및 아바타 영역 수정:

import 추가 (기존 import 블록에):
```tsx
import { ThemeToggle } from "@/components/layout/theme-toggle";
```

`<div className="ml-auto">` 블록을 아래로 교체:
```tsx
// Before (lines 58-62)
<div className="ml-auto">
  <Avatar className="h-8 w-8">
    <AvatarFallback>HR</AvatarFallback>
  </Avatar>
</div>

// After
<div className="ml-auto flex items-center gap-2">
  <ThemeToggle />
  <Avatar className="h-8 w-8">
    <AvatarFallback>HR</AvatarFallback>
  </Avatar>
</div>
```

변경 사항:
- `<div>` className에 `flex items-center gap-2` 추가
- `ThemeToggle` 삽입 (아바타 왼쪽)

- [ ] **Step 2: 개발 서버에서 시각 확인**

```bash
npm run dev
```

브라우저에서 http://localhost:3000 접속 후 확인:
- 헤더 우측에 Sun/Moon 아이콘 버튼이 아바타 왼쪽에 표시되는지
- 클릭 시 테마가 전환되는지 (배경색, 텍스트색 변경)
- 페이지 새로고침 후 선택한 테마가 유지되는지

- [ ] **Step 3: lint 확인**

```bash
npm run lint
```

Expected: 에러 없음.

- [ ] **Step 4: Commit**

```bash
git add components/layout/header.tsx
git commit -m "feat: add ThemeToggle to header layout"
```

---

### Task 5: E2E 테스트

**Files:**
- Create: `e2e/dark-mode.spec.ts`

- [ ] **Step 1: E2E 테스트 작성**

`e2e/dark-mode.spec.ts`:

```ts
import { test, expect } from "@playwright/test";

test.describe("다크 모드 전환", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("/");
  });

  test("기본_테마는_라이트_모드이다", async ({ page }) => {
    const html = page.locator("html");
    await expect(html).not.toHaveClass(/dark/);
  });

  test("토글_클릭_시_다크_모드로_전환된다", async ({ page }) => {
    await page.getByRole("button", { name: "테마 전환" }).click();
    const html = page.locator("html");
    await expect(html).toHaveClass(/dark/);
  });

  test("다크_모드에서_토글_클릭_시_라이트_모드로_돌아온다", async ({ page }) => {
    const toggle = page.getByRole("button", { name: "테마 전환" });
    await toggle.click(); // → dark
    await toggle.click(); // → light
    const html = page.locator("html");
    await expect(html).not.toHaveClass(/dark/);
  });

  test("페이지_새로고침_후_테마가_유지된다", async ({ page }) => {
    await page.getByRole("button", { name: "테마 전환" }).click();
    await expect(page.locator("html")).toHaveClass(/dark/);

    await page.reload();

    await expect(page.locator("html")).toHaveClass(/dark/);
  });
});
```

- [ ] **Step 2: E2E 테스트 실행**

```bash
npx playwright test e2e/dark-mode.spec.ts
```

Expected: 4 tests passed.

- [ ] **Step 3: 전체 테스트 스위트 통과 확인**

```bash
npm test -- --run && npx playwright test
```

Expected: 모든 단위 테스트 + E2E 테스트 통과.

- [ ] **Step 4: Commit**

```bash
git add e2e/dark-mode.spec.ts
git commit -m "test: add E2E tests for dark mode toggle"
```

---

### Task 6: 최종 검증 및 정리

- [ ] **Step 1: 빌드 최종 확인**

```bash
npm run build
```

Expected: 빌드 성공.

- [ ] **Step 2: lint + format 확인**

```bash
npm run lint && npm run format
```

Expected: 에러 없음.

- [ ] **Step 3: 변경된 파일이 format 후 달라졌다면 커밋**

```bash
git diff --name-only
# 변경 파일이 있으면:
git add -A && git commit -m "chore: apply formatting"
```
